pipeline {
  agent none
  stages {
    stage('Example Deploy') {
      agent {
        label 'linux'
      }
    stage('Example Deploy') {
      when {
        branch 'main'
        environment name: 'DEPLOY_TO', value: 'production'
      }
      steps {
        sh 'echo Deploying'
      }
    }
  }
}
